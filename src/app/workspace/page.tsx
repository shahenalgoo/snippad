/**
 * Default workspace page
 * Shows create new note options.
 * Check ./layout for the structure
 * 
 */

// Workspace components
import CreateNew from "./components/create-new/CreateNew";

export default function Workspace() {
    return <CreateNew />
}
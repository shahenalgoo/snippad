export enum NoteType {
    note = "note",
    code = "code"
}

export enum NoteStatus {
    published = "published",
    archived = "archived",
    trashed = "trashed"
}

export enum NoteFilter {
    all = "all",
    starred = "starred",
    archived = "archived",
    trash = "trash"
}

export enum SortDate {
    latest = "latest",
    oldest = "oldest"
}

